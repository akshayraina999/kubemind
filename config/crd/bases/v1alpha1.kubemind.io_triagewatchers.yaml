---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: triagewatchers.v1alpha1.kubemind.io
spec:
  group: v1alpha1.kubemind.io
  names:
    kind: TriageWatcher
    listKind: TriageWatcherList
    plural: triagewatchers
    singular: triagewatcher
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TriageWatcher is the Schema for the triagewatchers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TriageWatcher
            properties:
              cooldownPeriod:
                default: 30m
                description: 'CooldownPeriod defines how long an ongoing anomaly is
                  ignored after an alert ships. Example: "30m"'
                type: string
              slackSecretRef:
                description: SlackSecretRef provides the name of the Kubernetes Secret
                  string containing your webhook endpoint.
                type: string
              targetNamespace:
                description: TargetNamespace dictates which specific namespace this
                  worker instance monitors.
                type: string
            required:
            - slackSecretRef
            - targetNamespace
            type: object
          status:
            description: status defines the observed state of TriageWatcher
            properties:
              activeMonitors:
                description: ActiveMonitors represents how many distinct workloads
                  are currently under surveillance.
                type: integer
              lastScrapeTime:
                description: LastScrapeTime documents the timestamp of the last complete
                  evaluation sequence sweep.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
