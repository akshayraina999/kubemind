# This rule is not used by the project kubemind itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to v1alpha1.kubemind.io resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kubemind
    app.kubernetes.io/managed-by: kustomize
  name: triagewatcher-viewer-role
rules:
- apiGroups:
  - v1alpha1.kubemind.io
  resources:
  - triagewatchers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - v1alpha1.kubemind.io
  resources:
  - triagewatchers/status
  verbs:
  - get
